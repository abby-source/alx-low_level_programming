#include <stdio.h>
/**
 * main - A function that prits a line of code
 * Return: Always 0 (success)
 */
int main(void)
{	printf("with proper grammar, but the outcome is a piece of art,\n")
	return (0);
}
